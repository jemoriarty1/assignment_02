Write your reflection here.

See reflection.md for what makes a good reflection — be specific, use the
terminology from this assignment (package, module, ETL, coercion, public API,
relative import, accumulator, group by, unit vs. integration test), and make it
actionable.

I learned the difference between a relative import and an absolute import and
where is the right place to import them. Where I struggled was the integration
tests because even when all of my unit tests passes, a few integration tests
still failed.